# Interactive-OS-Scheduler-and-Memory-Manager
This C++ project simulates memory allocation (First Fit, Best Fit) and CPU scheduling algorithms (FCFS, SJF, Round Robin) using SFML for graphical visualization. It reads input from a JSON file and displays memory allocation results and Gantt charts to help users understand how different strategies manage resources.
